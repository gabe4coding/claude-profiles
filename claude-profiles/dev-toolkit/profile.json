{
  "_description": "Local dev environment with shell access; auto-approves edits for fast iteration",
  "mcpServers": {
    "fs": {
      "type": "stdio",
      "command": "echo",
      "args": [
        "mock"
      ]
    }
  }
}
